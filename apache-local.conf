<VirtualHost *:80>
    ServerName localhost
    DocumentRoot /var/www/html/gratulationsdienst

    <Directory /var/www/html/gratulationsdienst>
        AllowOverride FileInfo AuthConfig
        Require all granted
        Options -Indexes
    </Directory>

    Alias /mitgliederverwaltung /var/www/html/mitgliederverwaltung
    <Directory /var/www/html/mitgliederverwaltung>
        AllowOverride FileInfo AuthConfig
        Require all granted
        Options -Indexes
    </Directory>

    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
</VirtualHost>
